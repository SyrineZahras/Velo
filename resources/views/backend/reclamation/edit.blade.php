@extends('backend.master')

@section('content')


    <div class="container mt-2">
        <div class="row">
            <div class="col-lg-12 margin-tb">
                <div class="pull-left">
                    <h2>Edit Reclamation</h2>
                </div>
                <div class="pull-right">
                    <a class="btn btn-primary" href="{{ route('reclamations.index') }}" enctype="multipart/form-data">
                        Back</a>
                </div>
            </div>
        </div>
        @if(session('status'))
        <div class="alert alert-success mb-1 mt-1">
            {{ session('status') }}
        </div>
        @endif
        <form action="{{ route('reclamations.update',$reclamation->id) }}" method="POST" enctype="multipart/form-data">
            @csrf
            @method('PUT')
            <div class="row">
                <div class="col-xs-12 col-sm-12 col-md-12">
                    <div class="form-group">
                        <strong>Reclamation Name:</strong>
                        <input type="text" name="titrereclamation" value="{{ $reclamation->titrereclamation }}" class="form-control"
                            placeholder="Reclamation name">
                        @error('titrereclamation')
                        <div class="alert alert-danger mt-1 mb-1">{{ $message }}</div>
                        @enderror
                    </div>
                </div>
                <div class="col-xs-12 col-sm-12 col-md-12">
                    <div class="form-group">
                        <strong>Nom Event:</strong>
                        <input type="text" name="nomevent" class="form-control" placeholder="Event Reclamation "
                            value="{{ $reclamation->nomevent }}">
                        @error('nomevent')
                        <div class="alert alert-danger mt-1 mb-1">{{ $message }}</div>
                        @enderror
                    </div>
                </div>
                <div class="col-xs-12 col-sm-12 col-md-12">
                    <div class="form-group">
                        <strong>Description</strong>
                        <input type="text" name="description" value="{{ $reclamation->description }}" class="form-control"
                            placeholder="Company Address">
                        @error('description')
                        <div class="alert alert-danger mt-1 mb-1">{{ $message }}</div>
                        @enderror
                    </div>
                </div>
                <button type="submit" class="btn btn-primary ml-3">Submit</button>
            </div>
        </form>
    </div>
@endsection('content')